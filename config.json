{
  "name": "Calendar",
  "description": "Displays the future events",
  "elementName": "marcel-plugin-calendar",
  "files": ["index.html"],
  "icon": "",
  "props": [
    {
      "name": "url",
      "description": "Url of the server that send the events",
      "type": "string",
      "defaultValue": "http://localhost:5000"
    }
  ]
}